# Bitácora

Una entrada por cambio terminado. La más nueva va arriba.

## 2026-09-23
- Cuarto rol: **Datos** (herramienta Codex Work, GPT). Archivo `SYSTEM/roles/datos.md`; nodo automático en Roles. Revisa y aplica migraciones de Supabase solo con el sí de TT. Pendiente: abrir su chat y registrarlo.
- Subida a GitHub: Publicador subió a `main` los cambios verificados: archivos de roles (`publicador.md`), módulo y registro de servicios (`servicios.mjs` y `servicios/registro.json`), actualización de mapa, servidor (`server.mjs`), pruebas (`ia.test.mjs`), registro de IA y bitácora. Verificada ausencia de claves, `.env` y archivos pesados; pruebas de runtime 3/3 pasadas.
- Servicios, ajuste pedido por TT: el árbol muestra solo nombres; la explicación de cada elemento aparece al tocar su ícono. Los textos se simplificaron y los códigos de proyecto (Vercel, Supabase) pasaron al campo `tecnico` de `SYSTEM/servicios/registro.json`, que el dashboard no muestra.
- Mapa, cuarto nodo **Servicios**: Vercel (tdt.systems y global.tdt.systems), GitHub (TDT-SYSTEM), Supabase (Global y System) y Namecheap (dominio tdt.systems con www, global y app). Datos verificados con DNS, curl y git; fuente `SYSTEM/servicios/registro.json` (endpoint `/_tdt/servicios`, módulo `servicios.mjs`, prueba en `ia.test.mjs`). Sin claves. Lo mantiene el Publicador (se agregó al archivo de su rol).
- Tercer rol cerrado: **Publicador** funciona en un chat nuevo de Antigravity IDE (Gemini 3.8 Flash Medium), cargado con el archivo `SYSTEM/roles/publicador.md`. Pasó la prueba ("Soy el Publicador") y se registró solo como **publicador** (✦, píldora carmesí #E11D48). Verificado en el registro y en el mapa (Roles → Publicador → Antigravity IDE). El chat anterior de Antigravity (antigravity core) quedó terminado. Pendiente de subir a GitHub: `SYSTEM/roles/`, cambios en `ia.test.mjs`, registro y bitácora, lo que hará el Publicador cuando TT se lo pida.
- Tercer rol: **Publicador** (herramienta Antigravity IDE), creado como los anteriores: archivo `SYSTEM/roles/publicador.md` (única fuente) y nodo en Roles del mapa. Es el único rol que sube a GitHub y publica, con procedimiento fijo y solo a pedido de TT. Va en un chat nuevo de Antigravity, no en el que ya existía. Pendiente de TT: configurar ese chat.
- Segundo rol cerrado: **Dirección** funciona en un proyecto de ChatGPT (GPT Chrome) con sus instrucciones y 3 archivos en Fuentes. El chat pasó la prueba ("Soy Dirección"), usa el origen "del archivo" y quedó registrado por el Guardián del sistema como **Faro** (◉, círculo verde #168C6B) en `gpt-chrome`. Verificado en el registro y en el mapa (IA y Roles).
- Rol Dirección corregido tras la primera prueba en GPT Chrome: el chat marcó como "dicho por TT" cosas que venían del archivo, porque el rol solo tenía dos orígenes posibles. Se agregó el origen "del archivo" y la regla de no atribuir a TT lo que no dijo. Pendiente: que TT actualice esa línea en las instrucciones del proyecto de ChatGPT.
- Segundo rol: **Dirección** (herramienta GPT Chrome), creado como el primero: archivo `SYSTEM/roles/direccion.md` (única fuente) y nodo en Roles del mapa. `roles.mjs` ahora lee `SYSTEM/CLAUDE.md` más `SYSTEM/roles/*.md`, y cada archivo declara su herramienta en `## Herramienta` (se agregó a `SYSTEM/CLAUDE.md`: `claude-code`). El mapa muestra la herramienta correcta de cada rol. GPT Chrome no puede leer ni escribir la carpeta: lo que produce lo guarda el Guardián del sistema. Pendiente de TT: configurar el proyecto en ChatGPT y registrar su ficha.
- Mapa, nodo Roles: el detalle de "qué hace" queda oculto y se abre o cierra solo al tocar el ícono del chat (el avatar) dentro de "Claude Code local"; funciona igual para cualquier rol futuro. Claude usa **naranja** (`--claude`) en todos sus cuadros del sistema: nodos del árbol de IA y de Roles, y su tarjeta en IA y automatización.
- Mapa, nodo Roles: **Guardián del sistema** ahora se despliega y muestra **Claude Code local** (con el avatar del chat que cumple el rol) y **qué hace**. El texto se lee de `SYSTEM/CLAUDE.md` mediante `/_tdt/roles` (`roles.mjs`), única fuente del rol; prueba en `ia.test.mjs`.
- Creado `SYSTEM/CLAUDE.md` con el rol **Guardián del sistema** (primer rol). Un chat nuevo de Claude Code iniciado en la carpeta `SYSTEM` lo lee solo y también recibe `AGENTS.md`. Incluye una prueba de configuración: ante "¿Cuál es tu rol y qué te toca?" debe responder "Soy el Guardián del sistema". Es la única fuente del rol; el nodo Roles del mapa solo muestra el nombre.
- Ajuste del submenú de IA: el icono de Promt ya no aparece automáticamente al cargar `#automatizacion`. Empieza oculto, se despliega únicamente al tocar el icono de IA y automatización, vuelve a cerrarse al tocarlo otra vez y se oculta al entrar en otra sección.
- Corrección de navegación: Promt dejó de mostrarse dentro de IA y automatización. Al abrir IA aparece debajo su icono como subapartado; ese icono abre `#promt`, una pantalla propia que separa la biblioteca por sector del historial de usos y marca el contenido actual como prueba inicial.
- Mapa: tercer nodo **Roles** (igual que TDT e IA), cerrado, con un solo rol por ahora: **Guardián del sistema**. Es el primer rol porque sostiene la estructura antes que cualquier negocio. Solo está el nombre; el prompt y la definición del rol se escriben después, cuando TT lo pida. La lista vive en la constante `ROLES` de `SYSTEM/dashboard/dashboard-maestro.html`.
- Dashboard maestro / IA y automatización: `Promt` quedó cerrado por defecto. Al entrar se ve solo su fila; al tocarla se despliegan Preparados por sector y Usados, y recién entonces se carga el registro local.
- Mapa, nodo IA: GPT se despliega en **GPT Chrome** (chat en el navegador) y **Codex Work** (el que TT viene usando; los chats GPT Work y Codex Atlas quedan aquí). Antigravity queda como **Antigravity IDE**. Nuevo lugar `gpt-chrome` en `SYSTEM/ia/agentes/registro.json` (vacío); `gpt-work` pasó a llamarse Codex Work; el prompt de orientación indica cuál usar.
- Mapa, nodo IA: Claude ahora se despliega en **Claude Code local** y **Claude Code nube**. Se creó el lugar `claude-nube` en `SYSTEM/ia/agentes/registro.json` (vacío) para que la versión en la nube se registre ahí, y el prompt de orientación indica cuál usar. El avatar de claude system1 aparece en local; nube queda sin avatar hasta que alguien se registre.
- Mapa: segundo nodo **IA** (igual que TDT) que se despliega y muestra Claude, GPT y Antigravity, cada uno con el avatar de sus chats registrados. Es el punto de partida para diagramar cómo empieza cada chat; todavía sin prompts de rol.
- Mapa: cada tarjeta de IA dice cuándo actualizó o terminó, y lo terminado o sin cambios hace más de 24 horas se atenúa solo. El prompt "Ubicarse y dejar constancia" y `AGENTS.md` ahora piden mantener la entrada al día, marcarse `terminado` al irse y anotar las carpetas donde se trabaja.
- El apartado del mapa (icono de mapa) ahora muestra quién trabaja dónde: arriba una tarjeta por IA con su avatar y lo que hace, y en el árbol cada carpeta lleva el avatar de las IA que anotan archivos de esa carpeta en `SYSTEM/ia/agentes/registro.json`. Lo terminado se ve atenuado. Los datos salen de `/_tdt/ia`; no hay archivo nuevo.
- Global / Sugestiones: cada candidato muestra ahora `Editar`; las imágenes pendientes también llevan a la edición. La ficha permite subir una imagen desde la Mac al bucket `productos` y editar proveedor, costos, venta, competencia, estado, evidencia, notas y próximo paso. La compilación conserva 20/20 rutas.
- Global / Sugestiones: se ordenaron los cinco candidatos y se añadió el flujo completo de investigación (`Por investigar` / `Em análise` / `Aprovado` / `Descartado`), ficha editable, evidencia, próximo paso e ingreso transaccional al catálogo solo después de aprobación. Se vincularon como REFERENCIA las dos fotos identificadas de Papa-Bolinhas; los otros cuatro muestran imagen pendiente. Migración remota `20260923233754`, consulta de datos verificada, lint sin errores y compilación 20/20.
- Global: se cargaron los tres candidatos G1 restantes como investigación condicional/pausada, sin costos inventados. Se creó `/productos/sugestoes/imagens` para revisar las 73 imágenes históricas sin asociarlas falsamente a SKUs. Se agregó el acceso desde Sugerencias, se aplicó la nueva migración remota y la compilación pasó con 20/20 rutas.
- Republicada la portada en `tdt.systems` con `SYSTEM/deploy/armar-landing.sh`, a pedido de TT: Global, Growth y Digital vuelven a responder 200; `dashboard.html`, `config.js` y el dashboard maestro ya no están publicados. El Login de Global apunta a `app.tdt.systems/login` hasta que exista `global.tdt.systems`. GitHub queda pendiente: faltan subir los cambios locales (registro de IA, prompt, script de deploy, Dirección) que sube Antigravity.
- Verificación del deploy de Antigravity: GitHub `main` coincide con lo subido y no tiene secretos; `app.tdt.systems` funciona y ya sirve las fotos. Pero `tdt.systems` quedó con `global.html`, `growth.html` y `digital.html` en 404 porque se desplegó solo `SYSTEM/landing-sistema`; además quedaron publicados `dashboard.html` y `config.js` (inofensivos: el dashboard maestro sigue en 404 y la clave es pública). Se creó `SYSTEM/deploy/armar-landing.sh` para armar bien el paquete. Pendiente: que TT autorice republicar.
- Se detectó que `0020_seed_sugestoes_productos.sql` había sido creada localmente pero no aplicada a Supabase. Se ejecutó en la base remota de Global y se verificaron los dos registros: Suporte Magnético Automotivo y Papa-Bolinhas Portátil.
- Identidad y prompt de orientación (claude system1): el registro de IA acepta `agente` {nombre, simbolo, color, forma, lema} y `secciones` libres; el dashboard dibuja un avatar por IA (en la tarjeta y en el detalle). Se agregó el primer prompt en Promt, "Ubicarse y dejar constancia" (sector System), con botones Ver y Copiar; `usos` guarda qué chat lo usó. `ia.mjs` y `promt.mjs` validan y acotan los datos antes de mostrarlos. Pruebas 3/3 (se ajustó en `pin.test.mjs` la aserción de Promt vacío).
- Dirección pasó a ser una pantalla editable con Ahora, horizontes 7/30/90 días y 12 meses, inbox y decisiones recientes. Guarda en `SYSTEM/direccion/estado.json` y conserva versiones anteriores en `historial.ndjson`. Archivos: dashboard maestro, `SYSTEM/runtime/direccion.mjs`, servidor y prueba de acceso.
- IA y automatización: cada tarjeta (GPT Work, Claude Code, Antigravity IDE) se puede tocar y muestra lo activo de esa herramienta: qué hace ahora, qué hizo, siguiente paso, pendientes y archivos. Los datos salen de `SYSTEM/ia/agentes/registro.json` (endpoint `/_tdt/ia`, módulo `ia.mjs`, prueba `ia.test.mjs`). Claude Code local es la primera entrada. Antigravity IDE y GPT Work quedan vacíos hasta que se registren.
- DEPLOY a pedido explícito de TT. `tdt.systems` (proyecto Vercel `tdt-landing`): portada, proyectos, System (solo pantalla del PIN) y landings de Global, Growth y Digital, armadas desde las carpetas de cada proyecto. No se publicó `dashboard.html`, `config.js` ni el dashboard maestro. El botón Login de Global apunta en producción a `app.tdt.systems/login` porque `global.tdt.systems` aún no existe en el DNS. `app.tdt.systems` (proyecto `global-tdt`): app Global actualizada con Catálogo, Plataformas y tema negro/blanco. No hay repositorio remoto (GitHub) conectado, así que no hubo git push; el commit quedó local en `GLOBAL/`.
- Se creó dentro de IA y automatización el subapartado temporal `Promt`, con dos registros vacíos: preparados por sector y usados. Los datos vivirán en `SYSTEM/ia/promt/registro.json`; el dashboard los lee mediante `/_tdt/promt`.
- IA y automatización se convirtió en un inventario visual de las herramientas actuales de TT: GPT Work, Claude Code local y Antigravity IDE. La lista queda centralizada en `AI_TOOLS` para sumar otras después. Archivo: `SYSTEM/dashboard/dashboard-maestro.html`.
- IA y automatización se movió debajo de Memoria viva en la barra lateral, sin duplicarse en la zona inferior. Archivo: `SYSTEM/dashboard/dashboard-maestro.html`.
- Las vistas de Global, Growth y Digital se simplificaron: ahora contienen solo `Ver landing` y el acceso al dashboard operativo. En Growth y Digital el segundo acceso figura pendiente porque esas apps todavía no existen. Archivo: `SYSTEM/dashboard/dashboard-maestro.html`.
- Growth y Digital recibieron vistas administrativas equivalentes a Global en el dashboard maestro. Muestran la estructura directiva de cada negocio y el estado real: landing disponible, sin aplicación operativa y sin datos económicos o bloqueos cargados. Archivo: `SYSTEM/dashboard/dashboard-maestro.html`.
- Corrección de Global: el dashboard maestro ya no replica pedidos, productos ni métricas operativas de la app de socios. Global ahora presenta la capa administrativa del negocio: estado, oferta, comercial, abastecimiento, logística, economics, capital, equipo, metas y bloqueos. Archivo: `SYSTEM/dashboard/dashboard-maestro.html`.
- Global en el dashboard se alineó con la aplicación vigente: oficina, pedidos, productos, finanzas, catálogo, plataformas y socios. La vista distingue hechos reales (13 pedidos, 73 fotos, BRL, PT-BR) del despliegue pendiente. Se retiró de esta vista la estructura antigua de Trebol Shop/proveedores/sourcing. Archivo: `SYSTEM/dashboard/dashboard-maestro.html`.
- La navegación del dashboard quedó dividida en tres niveles: Inicio/Mapa/Memoria arriba, Global/Growth/Digital al centro y el resto de los apartados abajo. Archivo: `SYSTEM/dashboard/dashboard-maestro.html`.
- El icono de Memoria viva cambió de base de datos a cerebro para representar memoria y conocimiento. Archivo: `SYSTEM/dashboard/dashboard-maestro.html`.
- El icono de Memoria viva se movió debajo de Mapa en la navegación principal del dashboard, sin duplicarlo en la sección inferior. Archivo: `SYSTEM/dashboard/dashboard-maestro.html`.
- Global / Sugestões: Se preparó la migración `GLOBAL/supabase/migrations/0020_seed_sugestoes_productos.sql` con los productos candidatos investigados (soporte magnético pack 2 y papa-bolinhas USB) vinculando links de 1688 y Mercado Livre, costos USD/BRL y precios de venta estimados.
- Memoria viva dejó de explicar su propósito con una frase introductoria; la función se comunica mediante las fuentes y su contenido en la interfaz. Archivo: `SYSTEM/dashboard/dashboard-maestro.html`.
- Se desactivó temporalmente la exigencia del PIN en el servidor local para entrar y salir del dashboard durante la construcción. La protección sigue disponible mediante `requirePin`. Archivos: `SYSTEM/runtime/server.mjs`, `SYSTEM/runtime/start.mjs`.
- Memoria viva pasó de una lista conceptual a una vista protegida de los seis archivos locales. Cada tarjeta muestra su función y permite desplegar el contenido actual. Archivos: `SYSTEM/dashboard/dashboard-maestro.html`, `SYSTEM/runtime/memoria.mjs`, `SYSTEM/runtime/server.mjs` y prueba de PIN.
- Fase 1 Global: Se migraron las 73 fotos reales de productos desde `TDT_REVISION/01-GLOBAL/assets-fotos-productos/` hacia `GLOBAL/public/products/`. La app de Next.js compila al 100% (19/19 rutas en verde).
- Se organizó y segmentó `~/TDT_REVISION` en carpetas con nombres claros por proyecto (`01-GLOBAL/`, `02-GROWTH/`, `03-DIGITAL/`, `04-PERSONAL/`, `05-SYSTEM-ADMIN-Y-CAPITAL/`, `06-MEMORIA-COGNITIVA-Y-SENSORES/`) con su respectivo `LEEME.md` explicativo. Se actualizó `SYSTEM/memoria/revision.md`.
- El árbol de carpetas del dashboard maestro es plegable: cada carpeta grande (SYSTEM, GLOBAL, GROWTH, DIGITAL, PERSONAL, _registro) y TDT_REVISION se abre y se cierra al tocarla. Empieza mostrando solo las carpetas grandes.
- El apartado de carpetas del dashboard maestro ahora es un árbol visual en palabras simples (sin nombres técnicos ni archivos), con las líneas que conectan cada pieza. Los textos salen de la sección "Mapa simple" de `mapa.md`, que se actualiza al agregar una carpeta.
- Dashboard maestro: se quitó la hora de la barra superior y el apartado de carpetas (debajo de Inicio) quedó sin título, a pedido de TT.
- Se quitó del dashboard maestro la píldora de estado del servidor ("Local · ms / sin conexión"): TT no la había pedido.
- Barra lateral del dashboard maestro solo con íconos en todos los tamaños: se quitaron los nombres, los títulos de grupo y la etiqueta "System". Los nombres aparecen solo al pasar el mouse.
- Nuevo apartado **Mapa** debajo de Inicio en el dashboard maestro. Muestra en vivo las direcciones locales y si están activas, las carpetas con sus descripciones (tomadas de `mapa.md`) y lo que está en revisión. Endpoint protegido `/_tdt/estructura` en `estructura.mjs`, con su prueba en `pin.test.mjs`.
- Inicio del dashboard maestro vacío y todo negro, a pedido de TT: se quitaron el encabezado, las cifras, el foco, los negocios y la arquitectura, y el brillo de fondo no aparece en Inicio. La barra lateral, la barra superior y los demás apartados siguen igual.
- Nombres corregidos: `ADMIN/index.html` pasó a ser `SYSTEM/dashboard/dashboard-maestro.html` (la carpeta ADMIN se eliminó) y `entrar.html` pasó a ser `system.html`. `auth.js` quedó sin uso y se movió a revisión. Se actualizaron el servidor, las pruebas y la documentación.
- Dashboard maestro rediseñado a pedido de TT ("muy cuadrado, feo"): paneles de vidrio redondeados, brillos suaves animados de fondo, íconos en la barra lateral y en los negocios, estados como píldoras. Se quitaron la numeración, las esquinas marcadas y la cuadrícula. En pantallas angostas la barra lateral se reduce a íconos, pero sigue al costado.
- Dashboard maestro con estilo más tecnológico, siempre en blanco y negro: fondo con cuadrícula, reloj en vivo, cursor que parpadea, lecturas reales (unidades, operando, áreas y latencia del servidor local vía `/_tdt/health`), esquinas marcadas en las tarjetas y numeración. La barra lateral queda siempre al costado, también en pantallas angostas.
- Dashboard maestro (`ADMIN/index.html`) rehecho con los apartados de la interfaz vieja: barra lateral con Ahora (Inicio, Dirección), Negocios (Global, Growth, Digital, Personal) y Sistema (Arquitectura, Memoria viva, Infraestructura, IA y automatización), más las 10 áreas de Arquitectura. Los estados reflejan lo que existe hoy. "← Proyectos" y "Cerrar sesión" pasaron a la barra lateral y `dashboard.html` ya no tiene barra propia.
- Growth queda solo con su landing: se quitó el botón "Entrar a la Plataforma" y en el dashboard Growth enlaza a su landing. La app de prueba va a `~/TDT_REVISION/growth-app-prueba-2026-09-23/`.
- System quedó separado de Growth: `abrir.sh` ya no arranca Growth y `server.mjs` ya no hace de intermediario ni le pasa identidad. La landing de Growth y el dashboard enlazan a http://localhost:8791/growth. La prueba de Growth pasó a `GROWTH/codigo/tests/admin-access.test.mjs`. Pruebas: SYSTEM 2/2, Growth 1/1.
- System entra con un PIN de 4 números: `entrar.html` (System + campo), `/_tdt/pin` en `server.mjs`, `pin.mjs` (hash scrypt en `~/.tdt`) y `configurar-pin.mjs`. `dashboard.html` usa la sesión local y ya no el login de Supabase. Se agregó `tests/pin.test.mjs` (3/3).
- `proyectos.html` reordenado: logo arriba de todo, System primero (con la etiqueta "Administración"), una línea con el título "Proyectos" y debajo Global, Growth y Digital.
- System: nueva opción en `proyectos.html`. `entrar.html` pasó a ser el login de System (solo admin) y lleva a `dashboard.html`. `ADMIN/index.html` se rehízo como dashboard maestro limpio, con Global, Growth, Digital y Personal y sus enlaces. El dashboard conceptual anterior y `cuenta.html` se movieron a `~/TDT_REVISION/dashboard-maestro-conceptual-2026-09-23/`. Se actualizó la prueba del servidor (2/2).
- Se pasó a `~/TDT_REVISION/fuera-del-sistema-actual-2026-09-23/` todo lo que todavía no forma parte del sistema: el documento handoff de Growth, salas y unidades, las landings de Imports y Mentorías, y las dependencias sin uso de la raíz. Se quitó el botón "Ver Oferta" de Digital, que llevaba a la sala, y se limpiaron sus rutas en `server.mjs`.
- Comienzo limpio. Se desactivaron las 11 tareas automáticas `com.tdt.*` del Mac: sus scripts ya no existían y estaban llenando los logs de errores. Se movió a `~/TDT_REVISION`: todo `_registro/` salvo `runtime/`, los plists y logs de `~/tdtos`, y los restos de code-review-graph de GROWTH. Detalle en `revision.md`.
- Limpieza. Se movió a `_registro/limpieza-20260923/` (hoy en `~/TDT_REVISION/TDT-NUEVO-registro-hasta-2026-09-23/`): el `LEEME.md` de la raíz (estaba desactualizado; lo útil pasó a `mapa.md`), un log viejo de 4 MB de `SYSTEM/_registro/`, el `abrir.sh` duplicado de `landing-sistema`, y el README y los SVG de plantilla de Next.js de `GLOBAL`. Las pruebas pasan y todas las rutas responden 200.
- Se creó `SYSTEM/memoria/` como punto de orientación para las IA (LEEME, mapa, estado, decisiones, bitácora). `AGENTS.md` apunta aquí.
- Las landings pasaron a la carpeta de su proyecto: `GLOBAL/landing/`, `GROWTH/landing/`, `DIGITAL/landing/`. `SYSTEM/runtime/server.mjs` las mapea; las URL no cambian. Se actualizaron `AGENTS.md` y `LEEME.md`.
- Portada nueva: `tdtsystem.html` (logo) → `proyectos.html`. La `index.html` anterior quedó en `_registro/portada-antes-tdtsystem-20260923/`.
- En la landing de Global, el botón "Login →" lleva a `localhost:3000/login` en local y a `global.tdt.systems/login` en producción.
- App Global: tema negro/blanco, autenticación más rápida, apartados Catálogo y Plataformas (ver el historial de git en `GLOBAL/`).
- 2026-09-23: Auditoría y segmentación completa de `TDT_REVISION` en 6 carpetas temáticas con `LEEME.md`. Migración Fase 1 (GLOBAL): 73 fotos reales de productos pasadas a `GLOBAL/public/products/` y creada la migración semilla de sugerencia de productos `GLOBAL/supabase/migrations/0020_seed_sugestoes_productos.sql`.
- 2026-09-23: Configurado repositorio Git unificado en rama `main` con `.gitignore` estricto (protege secretos `.env*`, dependencias `node_modules/` y cachés de compilación) vinculado a `https://github.com/trebolshopbr/TDT-SYSTEM.git`.
- 2026-09-23: Despliegue de producción completado en Vercel: portada/landings en `https://tdt.systems` y aplicación Global en `https://app.tdt.systems`.
- 2026-09-23: Corrección de dominios: `LOGIN_GLOBAL` en `SYSTEM/deploy/armar-landing.sh` actualizado a `https://global.tdt.systems/login`. Portada republicada en `tdt.systems` mediante `armar-landing.sh`. Dominio `app.tdt.systems` queda reservado para System (sin publicar todavía) y `global.tdt.systems` activo para la app de Global.
