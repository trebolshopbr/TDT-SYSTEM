# Landing pública de TDT Global

- `global.html`: landing de Global con el botón "Login →" que lleva a la app del equipo (`/login`).

`SYSTEM/runtime/server.mjs` la sirve en http://localhost:8790/global.html. Las landings de Imports y Mentorías están en `~/TDT_REVISION/fuera-del-sistema-actual-2026-09-23/` hasta que se decida su lugar.
